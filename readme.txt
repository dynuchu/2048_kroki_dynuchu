The application uses readchar as an library.
To install it you need pip for python 3.5.

the following applications are needed to be installed:

sudo apt-get install python3-setuptools
sudo easy_install3 pip
sudo pip install readchar
